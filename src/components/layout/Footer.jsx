{sanitizedCopyright}
       
          {links && links.map((link, index) => (
            
              {link.text}
            
          ))}
        
      
    
  );
};

export default React.memo(Footer);